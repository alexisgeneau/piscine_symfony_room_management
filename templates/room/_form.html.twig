{{ form_start(form) }}
    {{ form_widget(form, {'attr': {'class': 'form-control'}}) }}
    <button class="btn bg-success text-white mt-2">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
