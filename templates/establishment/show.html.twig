{% extends 'base.html.twig' %}

{% block title %}Establishment{% endblock %}

{% block body %}
    <h1>Establishment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ establishment.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ establishment.name }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ establishment.address }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ establishment.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_establishment_index') }}" class="btn bg-info text-white mb-2 mt-2">back to list</a>

    <a href="{{ path('app_establishment_edit', {'id': establishment.id}) }}" class="btn bg-warning text-white mb-2 mt-2">edit</a>

    {{ include('establishment/_delete_form.html.twig') }}
{% endblock %}
